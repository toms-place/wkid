apiVersion: v1
kind: Service
metadata:
  name: {{ include "wkid.fullname" . }}
  labels:
    {{- include "wkid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wkid.selectorLabels" . | nindent 4 }}
