apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wkid.fullname" . }}-test-connection"
  labels:
    {{- include "wkid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wkid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
